@model IEnumerable<Category>
@{
    ViewData["Title"] = "Index Category";
    Layout = "~/Views/Shared/_Layout.cshtml"; 
}

<h1>Index Gategory</h1>


<tabel class="table table-bordered table-striped w-100">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
        <tr />
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
            </tr>
        }
    </tbody>
</tabel>